node_modules/
.next/
out/
build/
dist/
coverage/
.DS_Store
*.log
*.tsbuildinfo
.env*
!.env.example
.vercel

# Locally-installed agent skills — vendored markdown, reinstallable via
# `npx skills add`. Not app code.
.agents/
.claude/
skills-lock.json

# NOTE: .mcp.json is deliberately NOT ignored. It is Claude Code's
# project-scope MCP config (equivalent to `claude mcp add --scope project`),
# so the Supabase connection travels with the repo. It holds no secrets —
# the project ref is already public via NEXT_PUBLIC_SUPABASE_URL.
